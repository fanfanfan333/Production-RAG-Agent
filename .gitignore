# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (frontend)
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# ── Backend (Python) ──────────────────────────────────────────────
backend/.env
backend/**/__pycache__/
backend/**/*.pyc
backend/venv/
backend/.venv/
backend/*.egg-info/
backend/alembic/versions/__pycache__/

# ── IDE / local tooling state ─────────────────────────────────────
.idea/
.vscode/
.workbuddy/
.workbuddy-ai/

# ── 本地临时产物 / 一次性调试脚本（不入库）────────────────────────
.e2e_tmp/
.login.html
probe_*.py
probe_*.txt
/NVIDIA Corporation/

# ── 会话调试输出（根目录不允许留，即便残留也不入库）──────────────
/audit*.txt
/cleanup_result.txt
/final_check.txt
/msg_check.txt
/git_push*.txt
/wmi_*.txt
/root*.txt

# ── 本机构建缓存 / 依赖镜像产物（不入库）────────────────────────
.next-verify/
backend/_wheels/
backend/_fetch_wheels.sh
.npmrc
git_out.txt
ls_out.txt
copy_result.txt
git_stage*.txt
*_tmp_del.txt
# git 修复过程的临时产物
.git_broken/
fix.txt
push_result*.txt
pull_info*.txt
merge_info*.txt
diag*.txt
shallow.txt
git_author.txt
git_commit*.txt
*_probe.txt

# 会话临时输出 / 一次性侦察产物（不入库）
.scratch/
backend/_recon.txt

# ── 敏感内容：口令清单 / 个人简历 / 含真实漏洞细节的审计报告（严禁入库）──
# test-accounts.md 含真实登录口令，即便已被 e2e 脚本泄漏也不再新增暴露面
test-accounts.md
docs/resume/
docs/简历项目说明与补全建议.md
deliverables/

# ── 安全审计 / 诊断类报告（描述真实可被利用的隔离漏洞细节，不上传）──────
docs/security_audit_rag_isolation.md
docs/audit_parsing_and_vision.md
docs/verification_golden_and_multimodal.md

# ── 本机依赖镜像 / 大体积产物（262MB，严禁入库）──────────────────────
_qa_pylibs/

# ── 根目录会话侦察 / 临时产物（统一前缀 _）──────────────────────────
/_*
backend/_iq_t1.txt
eval_cases.txt

# ── 运行时上传产物（含真实文档与页面原图，严禁入库）──────────────────
uploads/

# ── backend 侧一次性侦察 / 日志产物（统一前缀 _）──────────────────────
backend/_*.txt
backend/scripts/_*.py